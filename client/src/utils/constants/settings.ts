import { IColourScheme } from "../../types/settings";

export const SET_DARK_MODE = "SET_DARK_MODE";

export const SET_LIGHT_MODE = "SET_LIGHT_MODE";

export const darkScheme: IColourScheme = {
    backgroundColour: "black",
    textColour: "white",
};

export const lightScheme: IColourScheme = {
    backgroundColour: "white",
    textColour: "black",
};